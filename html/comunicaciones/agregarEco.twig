{# empty Twig template #}


<div class="modal-content">
	<div class="modal-header">
	  <button type="button" class="close" ng-click="cancel()"></button>
	  <h4 class="modal-title">Agregar Eco</h4>
	</div>

	<form>
		<div class="modal-body"  >
			<table class="tablaModal">
				<tr>
					<td>Título</td>
					<td>
						<input type="text" ng-model="form.tituloEco" ng-init="form.tituloEco = '' " name="tituloEco" placeholder="Título">
			    		<input type="hidden" ng-model="form.section" ng-init="form.section = 'agregarEco' "  name="section" >
					</td>
				</tr>
				<tr>
					<td>Archivo PDF</td>
					<td>
						{# INSERTAR MACRO DE IMAGENES #}
						<div class="subirFoto">
			   				[% import 'subirImagen.twig' as subirImagen%]
							[[ subirImagen.input() ]]
						</div>
					</td>
				</tr>
			</table>
		</div>
		<div class="modal-footer">
	      	<button type="button" class="btnOutline cancelar marginR btn-default"  ng-click="cancel()">Cancelar</button>
	      	<button type="button" ng-click="submitForm('[[ paths.PATH_CONTROLLER_COMUNICACIONES ]]controladorAdminModel.php?language=[[ lang ]]', form)"  class="btn publicar btn-primary">Publicar</button>
	   	</div>
   	 </form><!-- form -->
</div>